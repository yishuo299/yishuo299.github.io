---
import BaseLayout from "../../layouts/BaseLayout.astro";

const screenshots = [
  ["登录入口", "统一登录页负责身份识别，登录后按角色进入不同后台。", "https://piv.cc.cd/file/BQACAgUAAyEGAASLVN5eAAJsYGqgB5P-NZ4Y4NCCkap6SHbpYjqrAAJjJAACnMUBVZYskB7sMyBMPQQ.jpg"],
  ["教师开课", "教师与教务人员可以维护课程开设信息、时间、地点与容量。", "https://piv.cc.cd/file/BQACAgUAAyEGAASLVN5eAAJsYWqgB9er1sK1Jj1w1f1OvNIDaYdHAAJnJAACnMUBVWabRh7lWkr-PQQ.jpg"],
  ["学生课表", "学生选课成功后生成个人课表，便于查看每周课程安排。", "https://piv.cc.cd/file/BQACAgUAAyEGAASLVN5eAAJsYmqgB_HNLPZUsYxZBjdt57zf0rMvAAJpJAACnMUBVYTWe5FPwM9ePQQ.jpg"],
  ["成绩查询", "成绩发布后，学生可以查询课程成绩与学习结果。", "https://piv.cc.cd/file/BQACAgUAAyEGAASLVN5eAAJsY2qgCAVM2GpnHUawdGCBWhWhJT2XAAJqJAACnMUBVfAAATv_pJrKTz0E.jpg"],
  ["权限管理", "管理员可以维护角色权限，控制不同用户可访问的功能模块。", "https://piv.cc.cd/file/BQACAgUAAyEGAASLVN5eAAJsZGqgCCa2KXNPXDk5RLWkGIq3tfzwAAJrJAACnMUBVRqYb8c7WcPOPQQ.jpg"],
  ["操作日志", "系统记录关键操作，便于排查问题和追踪管理行为。", "https://piv.cc.cd/file/BQACAgUAAyEGAASLVN5eAAJsZ2qgCQu4yDpjFiPF8uF_rG7rJi18AAJ_JAACnMUBVQOdG6kG4Mn8PQQ.jpg"]
];

const selectionCode = `if (course.getSelectedCount() >= course.getCapacity()) {
  throw new BusinessException("课程容量已满");
}
if (selectionMapper.exists(studentId, courseId)) {
  throw new BusinessException("不能重复选择同一门课程");
}
if (selectionMapper.hasTimeConflict(studentId, course.getWeekDay(), course.getSection())) {
  throw new BusinessException("课程时间冲突");
}`;
---

<BaseLayout sideBarActiveItemID="projects">
  <main class="course-article">
    <a class="back-link" href="/#projects">← 返回项目列表</a>
    <section class="hero-card">
      <p class="eyebrow">Template 01 · 完整展示版</p>
      <h1>选课与成绩管理系统</h1>
      <p class="lead">这是一个基于 Spring Boot + Vue 3 的前后端分离教务管理项目，围绕“课程开设、学生选课、教师录入成绩、教务审核、管理员维护系统”建立完整业务闭环。</p>
      <div class="tech-line">
        {["Spring Boot", "Vue 3", "MySQL", "JWT", "Element Plus", "MyBatis Plus"].map((tag) => <span>{tag}</span>)}
      </div>
    </section>

    <section class="content-grid">
      <article class="panel wide">
        <h2>项目定位</h2>
        <p>本项目面向学校教务场景，目标不是简单展示几个页面，而是把选课系统中最常见、最核心的流程做成可管理、可追踪、可扩展的后台系统。系统通过角色权限区分学生、教师、教务管理员和系统管理员，让不同身份只看到自己需要处理的功能。</p>
        <p>前端使用 Vue 3 和 Element Plus 构建统一后台界面，后端使用 Spring Boot 提供接口与业务规则，数据库使用 MySQL 保存课程、用户、选课、成绩和日志等核心数据。</p>
      </article>

      <article class="panel">
        <h2>学生端</h2>
        <p>学生可以登录系统后浏览课程、筛选课程、提交选课、查看课表、查询成绩。系统在选课时会进行容量、重复选择和时间冲突等校验，避免无效数据进入数据库。</p>
      </article>

      <article class="panel">
        <h2>教师端</h2>
        <p>教师主要负责查看授课课程、维护课程信息、录入学生成绩，并在成绩完成后提交给教务审核。成绩模块可以根据平时、期中、期末等分项计算总评。</p>
      </article>

      <article class="panel">
        <h2>教务端</h2>
        <p>教务管理员维护课程、开课、教室、学期、班级等教学基础数据，同时负责成绩审核、选课开放控制和异常处理，是系统业务流转的核心角色。</p>
      </article>

      <article class="panel">
        <h2>管理员端</h2>
        <p>系统管理员负责用户管理、角色权限分配、菜单控制和操作日志查询。通过日志记录，可以看到用户、请求地址、IP、耗时、状态和异常信息。</p>
      </article>
    </section>

    <section class="code-card">
      <h2>核心业务示例：选课校验</h2>
      <pre><code>{selectionCode}</code></pre>
      <p>选课不是简单新增一条记录，而是需要在提交前完成业务判断。这样可以保证学生课表、课程容量和后续成绩录入都处于一致状态。</p>
    </section>

    <section class="screens">
      <h2>项目界面预览</h2>
      <div class="shot-grid">
        {screenshots.map(([title, desc, src]) => (
          <figure>
            <img src={src} alt={title} loading="lazy" />
            <figcaption><strong>{title}</strong><span>{desc}</span></figcaption>
          </figure>
        ))}
      </div>
    </section>

    <section class="summary-card">
      <h2>项目总结</h2>
      <p>这个版本适合作为正式项目介绍模板。它强调系统完整度、技术架构和角色职责，适合放在作品集、GitHub README 或求职项目说明中。整体语言稳重清晰，重点突出“我完成了什么功能、用了什么技术、解决了什么业务问题”。</p>
    </section>
  </main>
</BaseLayout>

<style>
  .course-article { width: min(1120px, 92vw); margin: 0 auto; padding: 96px 0; }
  .back-link { display: inline-flex; margin-bottom: 24px; color: var(--ink-2); text-decoration: none; }
  .hero-card, .panel, .code-card, .summary-card, .screens figure { border: 1px solid var(--line); background: linear-gradient(135deg, rgba(255,255,255,.08), rgba(255,255,255,.025)); box-shadow: 0 24px 80px rgba(0,0,0,.18); backdrop-filter: blur(18px); }
  .hero-card { padding: clamp(34px, 6vw, 70px); border-radius: 30px; }
  .eyebrow { color: var(--accent); letter-spacing: .24em; text-transform: uppercase; }
  h1 { margin: 10px 0 18px; color: var(--ink); font-size: clamp(42px, 8vw, 86px); line-height: .98; }
  h2 { margin: 0 0 16px; color: var(--ink); }
  .lead { max-width: 820px; color: var(--ink-2); font-size: 19px; line-height: 1.9; }
  .tech-line { display: flex; flex-wrap: wrap; gap: 10px; margin-top: 28px; }
  .tech-line span { padding: 8px 12px; border-radius: 999px; background: rgba(255,255,255,.08); color: var(--ink); }
  .content-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 18px; margin-top: 24px; }
  .panel { padding: 28px; border-radius: 24px; }
  .panel.wide { grid-column: 1 / -1; }
  p { color: var(--ink-2); line-height: 1.9; }
  .code-card, .summary-card, .screens { margin-top: 24px; }
  .code-card, .summary-card { padding: 30px; border-radius: 24px; }
  pre { overflow-x: auto; padding: 20px; border-radius: 18px; background: rgba(0,0,0,.35); color: #eaf1ff; }
  .shot-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 18px; }
  .screens h2 { margin: 42px 0 18px; }
  figure { margin: 0; overflow: hidden; border-radius: 22px; }
  figure img { display: block; width: 100%; aspect-ratio: 16 / 9; object-fit: cover; }
  figcaption { display: grid; gap: 6px; padding: 16px; color: var(--ink-2); }
  figcaption strong { color: var(--ink); }
  @media (max-width: 760px) { .content-grid, .shot-grid { grid-template-columns: 1fr; } }
</style>
